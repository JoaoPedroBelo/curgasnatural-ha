# Test dependencies for the CUR Gás Natural integration
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-homeassistant-custom-component>=0.13.0
pytest-cov>=4.1.0

# Home Assistant
homeassistant>=2024.1.0

# Integration dependencies
aiohttp>=3.9.0
aioresponses>=0.7.6

# Recorder dependencies. pytest-homeassistant-custom-component does not pull these
# in, but tests/test_init.py needs a working recorder to assert the long-term
# statistic is actually imported. Without them that whole module errors out.
psutil-home-assistant>=0.0.1
SQLAlchemy>=2.0.0
fnv-hash-fast>=0.5.0
